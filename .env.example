# ============ 环境变量示例 ============
# 复制为 .env（或 .env.local）并按需填写；.env* 已在 .gitignore 中忽略（除 .env.example）。

# 图片代理基础地址（用于 html-to-image 导出长图时跨域加载远程角色图标）。
# - 本地开发 / 预览：留空即可，默认走 Vite 中间件 /__img（见 vite.config.ts 的 imageProxy 插件）。
# - 部署到 GitHub Pages 等纯静态托管：必须指向你的 Cloudflare Worker / Vercel Serverless 函数地址（末尾不带斜杠）。
#   例（Cloudflare Worker）：
#   VITE_IMAGE_PROXY_BASE=https://botc-image-proxy.your-subdomain.workers.dev
#   例（Vercel Serverless）：
#   VITE_IMAGE_PROXY_BASE=https://your-project.vercel.app/api/image-proxy
VITE_IMAGE_PROXY_BASE=https://clock-tower-review-tools.vercel.app/api/image-proxy
