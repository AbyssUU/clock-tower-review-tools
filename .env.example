# ============ 环境变量示例 ============
# 复制为 .env（或 .env.local）并按需填写；.env* 已在 .gitignore 中忽略（除 .env.example）。

# 图片代理基础地址（用于 html-to-image 导出长图时跨域加载远程角色图标）。
#
# 绝大多数情况「无需设置」，代理地址会按构建模式自动选择（见 src/lib/proxy.ts）：
#   - 本地开发 / 预览（npm run dev / preview）：自动走 Vite 中间件 /__img。
#   - Electron 桌面（build:electron）：自动走主进程自定义协议 botc-img://image。
#   - Vercel（build:vercel）：自动走同源函数 /api/image-proxy（生产/预览域名自适应）。
#
# 仅当部署到「纯静态托管且代理在另一个域名」时才需手动指定，例如 GitHub Pages 指向 Cloudflare Worker：
#   VITE_IMAGE_PROXY_BASE=https://botc-image-proxy.your-subdomain.workers.dev
#
# VITE_IMAGE_PROXY_BASE=
